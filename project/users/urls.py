from django.urls import path
from project.users import views

urlpatterns = [
    # path('login/', views.login),
    # path('entrar/', views.auth),
    # path('', views.users),
    # path('<int:id>/', views.user),
    # path('criar/', views.create_user),
    # path('salvar-criacao/', views.save_creation),
    # path('<int:id>/editar/', views.update_user),
    # path('salvar-edicao/', views.save_edition),
    # path('<int:id>/deletar/', views.delete_user),
    # path('salvar-delecao/', views.save_deletion)
]
